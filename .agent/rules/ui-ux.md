---
trigger: always_on
---

# UI / UX Consistency Rule (Workspace Rule)

- Maintain mobile-first layouts.
- Follow existing glassmorphism theme and CSS variables.
- Avoid adding new UI patterns unless necessary.
- Search-first experience must remain the primary interaction.
- Prompt reuse is prioritized over prompt editing.
- Empty states, loading states, and error states are mandatory.
